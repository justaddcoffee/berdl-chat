[project]
name = "berdl-chat"
version = "0.1.0"
description = "Natural language chat interface to the BERDL Data Lakehouse"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.77.0",
    "httpx>=0.28.1",
    "nicegui>=3.6.1",
    "python-dotenv>=1.2.1",
]
